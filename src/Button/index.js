import { StyledButton } from "./styled";

const Button = ({ navigation }) => {
    return (
        <StyledButton navigation={navigation}>
            Request Invite
        </StyledButton>
    )
};

export default Button;